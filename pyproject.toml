[project]
name = "bias-ext-security"
version = "0.1.0"
dependencies = ["bias-core>=0.1,<0.2"]
[project.entry-points."bias.extensions"]
security = "bias_ext_security.backend.ext:extend"
[tool.setuptools.packages.find]
where = ["."]
include = ["bias_ext_security*"]
